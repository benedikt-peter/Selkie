#include "selkie/test_system.hpp"

#include <spdlog/spdlog.h>

namespace selkie
{
  void TestSystem::Update(World& world, Time time)
  {
//    spdlog::info("Update (total: {}s, delta: {}s, tick: {})", time.total_time, time.delta_time, time.tick.number);
  }

}